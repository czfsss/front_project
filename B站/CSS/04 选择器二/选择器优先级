全局选择器：*
元素选择器：标签名称
类选择器：class属性名称（.）
ID选择器：id属性名称（#）

优先级从高到低为：内联样式 > ID选择器 > 类选择器 > 元素选择器 > 全局选择器

注意：
1. 内联样式：在标签内部使用style属性定义的样式
2. ID选择器：在标签内部使用id属性定义的样式
3. 类选择器：在标签内部使用class属性定义的样式
4. 元素选择器：在标签内部使用标签名称定义的样式
5. 全局选择器：在标签内部使用*定义的样式

